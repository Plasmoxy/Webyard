{
  "name": "p5js_base_ts",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "run-p start-compile start-run",
    "start-compile": "tsc --watch",
    "start-run": "browser-sync start -s 'build' -w -f 'build'"
  },
  "devDependencies": {
    "@types/p5": "^0.9.1",
    "browser-sync": "^2.26.12",
    "npm-run-all": "^4.1.5",
    "typescript": "^4.0.2"
  },
  "dependencies": {
    "p5": "^1.1.9"
  }
}
