
import React, {FC} from 'react'
import { Layout } from '../components/layout'

interface Props {
  
}

const HeloPage: FC<Props> = (props) => {
  return <Layout>
    a
    <div style={{height: 10000}} />
  </Layout>
}

export default HeloPage